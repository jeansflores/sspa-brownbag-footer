import React from "react";
import * as S from "./Footer.styles";

const Navbar = () => {
  return (
    <S.CustomFooter style={{ textAlign: "center" }}>
      Microfronted Footer Here!
    </S.CustomFooter>
  );
};

export default Navbar;
